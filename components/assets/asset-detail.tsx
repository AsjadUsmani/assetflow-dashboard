"use client";

// Deprecated: asset detail UI now lives in `app/(dashboard)/assets/[id]/asset-detail-client.tsx` and
// uses real backend data via `lib/services/assets`. This component is kept as an empty
// placeholder to avoid breaking imports, and no longer uses any mock data.

export function AssetDetail() {
  return null;
}
